PREFIX : <http://opencirm.org#>

SELECT * 
WHERE {
  ?property a :OWLObjectProperty .
  ?property :hasColumnMapping ?column .
  {
     ?column a :DBPrimaryKey
  }
  UNION 
  {     
     ?column a :DBNoKey
  }
  ?column :hasTable ?table
}