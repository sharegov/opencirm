/**
 * 
 */
/**
 * Standalone Tests of external services, where the tests have dependencies on open/mdcirm code.<br>
 * (These are directly executable and not junit tests that can be integrated into a regression test suite)
 * 
 * @author Thomas Hilpold
 *
 */
package standalone.externalservices;